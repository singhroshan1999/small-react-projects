import React from 'react';
import Selected from './selected';

const ListSelected = (porps) =>{
    return (
        <React.Fragment>
            <Selected />
            <Selected />
        </React.Fragment>
    )
}

export default ListSelected;